// JavaScript 코드는 여기에 추가하세요.

// 변수 선언
var 변수명 = 값;

// 함수 선언
function 함수명(매개변수) {
    // 함수 내용
}

// 이벤트 핸들러 등록
document.getElementById('요소ID').addEventListener('이벤트', function() {
    // 이벤트 핸들러 내용
});

// 조건문 (if 문)
if (조건) {
    // 조건이 참일 때 실행되는 코드
} else {
    // 조건이 거짓일 때 실행되는 코드
}

// 반복문 (for 문)
for (var i = 0; i < 배열.length; i++) {
    // 배열 요소에 대한 반복 작업
}

// 콘솔에 로그 출력
console.log('메시지');

// 주석은 코드를 설명하기 위해 사용하세요.
